# Dockerfile.dev
FROM node:20.9.0-alpine

WORKDIR /app

# 安装开发工具和依赖
RUN apk add --no-cache git

# 复制 package.json 和 package-lock.json
COPY package*.json ./

# 安装项目依赖（包括开发依赖）
RUN npm install

# 复制项目文件
COPY . .

# 暴露开发服务器端口
EXPOSE 3000

# 启动开发模式
CMD ["npm", "run", "dev"]